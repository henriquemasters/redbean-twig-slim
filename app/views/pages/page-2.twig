{% extends 'template.twig' %}

{% block content %}
    <main class="showcase-shell slim-page">
        <section class="feature-page reveal">
            <span class="eyebrow">Recurso demonstrado</span>
            <h1>Persistencia pragmatica para CRUDs pequenos.</h1>
            <p>Os models encapsulam chamadas RedBeanPHP para consultas, atualizacoes e stores. Isso reduz boilerplate em MVPs e mantem a persistencia fora dos controllers.</p>
            <div class="code-card">User::all() | Role::save($data) | Permission::getCell(...)</div>
            <a class="text-link" href="{{ base_url('') }}">Voltar para a vitrine</a>
        </section>
    </main>
{% endblock %}
